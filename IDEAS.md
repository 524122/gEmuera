# 项目工作约定

本文用于给后续 Agent 快速理解本项目的开发背景、默认目标和协作规则。开始任何任务前，先阅读本文和 `CODE_MAP.md`。

## 项目背景

本项目使用 Godot 4.7 + C# 复刻/移植 Emuera 模拟器核心。

主要参考实现：

- `E:\MyCode\Era\emuera_lazyloading_selfmodified_version-main-skiasharp`
- `E:\MyCode\GodotCode\gemuera\uEmuera-0.2.9d`
- `E:\MyCode\GodotCode\gemuera\XEmuera-0.5.1`

默认开发重点：

- 首要运行和测试目标是手机端，所有方案默认以导出 APK 后可用为准。
- 桌面端可以作为调试入口，但不能替代 Android/APK 验证结论。
- 默认测试游戏目录：`E:\Godot_v4.6.2-stable_mono_win64\snake\EraTW-Magic_DLC-update_base`。
- 涉及 Godot 架构、UI、性能、平台适配时，需要使用 `godot-master` skill 辅助判断。

## 代码地图

项目已经维护一份动态代码地图：[CODE_MAP.md](CODE_MAP.md)。

使用规则：

- 开始检索源码前，优先阅读 `CODE_MAP.md`，减少无目的搜索。
- 修改或新增 `Scripts/**/*.cs` 后，必须判断是否需要同步更新 `CODE_MAP.md`。
- 如果变更影响入口流程、目录职责、接口契约、核心函数或常见问题定位，必须更新 `CODE_MAP.md`。
- 如果只是局部实现细节、注释、格式或不改变定位信息的小修，可以不更新，但最终说明中要明确判断结果。

## 代码修改原则

总体原则：优先保持现有效果和兼容行为，不为了重构而重构。

执行要求：

- 不要破坏已有功能表现，尤其是原 Emuera 行为、Snake 兼容逻辑、移动端布局、输入流程、图片/精灵渲染。
- 修改前先定位责任边界：UI 层、GameView、GameProc、GameData、Content、uEmuera 兼容层分别处理不同问题。
- 性能默认按手机端考虑，避免在热路径中增加无界分配、全量扫描、同步 I/O、逐帧重建节点或纹理。
- 需要扩展能力时，优先沿用当前项目已有模式；只有能明显降低复杂度或重复时才新增抽象。
- 复杂逻辑需要写中文注释，说明原因、边界和兼容性要求；不要写“把 A 赋值给 B”这类无信息注释。
- 对跨线程、UI 队列、输入等待、日志导出、资源加载、ColorMatrix、存档兼容相关代码要额外谨慎。
- 修改后尽量做针对性验证；如果无法验证 Android/APK 行为，必须在最终回复中明确说明。

## 问题修复流程

当用户提供日志时，先做诊断再改代码。

常见日志文件名包括：

- `emuera.log`
- `emuera_xxx.log`
- `gemuera_xxx.log`
- `emuera_startup_debug`
- `chara_debug` 或类似角色调试日志

日志处理规则：

- 如果同时存在 `emuera_xxx.log` 和 `gemuera_xxx.log`，必须先确认两个日志的日期/时间对应同一次测试。
- 如果两份日志日期不一致，不要基于它们修复代码；先向用户确认并要求提供同一次运行的日志。
- 不要只根据单行报错下结论，优先结合调用链、平台、游戏目录、最近变更和复现路径判断。
- 修复后要说明根因、改动位置、验证方式，以及是否需要用户重新导出 APK 测试。

## 日志与诊断

项目已有日志和诊断体系，虽然还在演进，但应优先使用现有能力。

相关位置：

- 配置文件：[config.toml](config.toml)
- 诊断模块：`Scripts/Diagnostics/`
- 日志桥接：`Scripts/GenericUtils.cs`
- 诊断导出：`DiagnosticLogExporter`、`GenericUtils.ExportDiagnosticPackage`

使用规则：

- 新增诊断信息前，先检查 `RuntimeDiagnosticsConfig` 和 `DiagnosticLogRouter` 是否已有分类/开关。
- 日志要能帮助定位问题，避免在高频路径刷屏。
- 用户要求排查移动端问题时，优先考虑导出诊断包或收集 APK 运行日志。

## Action Maps 操作日志

项目使用 `action_maps/` 保存 AI 或程序执行任务时产生的详细操作日志。它的目标是让后续 review、回退、PR 审核和多 AI 协作都有可追溯依据。

重要边界：

- `action_maps/` 是本地操作日志目录，只用于当前工作机上的 AI 操作审计。
- 不要将 `action_maps/` 提交到 GitHub，不要把它放进 PR，也不要用 `git add -f action_maps/` 强行加入版本控制。
- GitHub/PR 中只需要写本次任务的摘要、验证结果和风险说明；完整逐步操作日志保留在本地 `action_maps/`。
- `.gitignore` 必须忽略 `action_maps/`，防止误提交。

维护规则：

- 每次 AI 或程序执行会修改文件的任务，都必须在 `action_maps/` 中新增或更新一份中文操作日志。
- 文件命名建议：`YYYY-MM-DD-NNN-任务简述.md`，例如 `2026-05-27-001-create-action-maps.md`。
- 一个任务对应一份日志；不要把多个无关任务混写到同一份日志。
- `action_maps/` 内日志文件数量不得超过 30 个。
- 创建新日志前必须统计现有日志数量；如果新增后会超过 30 个，先整理最早的旧日志，或向仓库主人确认归档/删除策略，不能直接产生第 31 个日志文件。
- 日志必须足够详细，不能只写“修改了文件”这类无效描述。
- 如果任务只做分析、不修改文件，可以不创建日志；但如果分析结果会作为后续修复依据，建议创建日志。

单份操作日志必须包含：

- 任务目标：用户要求和本次执行范围。
- 初始状态：当前分支、工作区状态、相关已有改动、日志文件计数。
- 操作步骤：读取了哪些文件、执行了哪些命令、每一步得到什么结论。
- 修改记录：每个被修改文件的具体改动、原因、影响范围。
- 验证记录：执行过的命令、结果、失败原因、未验证内容。
- `CODE_MAP.md` 判断：是否需要更新代码地图，以及原因。
- 风险评估：可能影响的模块、平台、兼容行为。
- 回退建议：如果本次改动出错，应该优先回退哪些文件或哪些 hunk。

日志不是 Git 的替代品，也不是仓库历史的一部分。真正回退仍应优先依赖 Git diff、commit、PR 和明确的反向 patch；`action_maps/` 只负责在本地记录“为什么这么改、改了哪里、怎么验证、怎么回退”。

## 协作开发

后续可能会多人或多个 AI 同时参与开发，因此 GitHub 协作必须保守、可追踪、可回退。

仓库信息：

- GitHub 仓库：[wwwXiaoHan17/gEmuera](https://github.com/wwwXiaoHan17/gEmuera)
- 默认协作分支：`dev`
- 仓库主人拥有最终合并、回退、发布和分支保护决策权。

分支规则：

- 除仓库主人明确要求外，不要直接向 `dev` 或主分支提交代码。
- 每个任务从最新 `dev` 新建独立工作分支，建议命名为 `ai/<任务简述>` 或 `fix/<问题简述>`。
- 开始任务前先查看当前分支、工作区状态和远端状态，避免覆盖他人改动。
- 如果发现同一文件已有他人未合并改动，先阅读差异并判断是否冲突；无法确认时停止并询问。
- 禁止擅自执行破坏性 Git 操作，例如强制推送、硬重置、删除远端分支、回滚他人提交。

提交和 PR 规则：

- 普通协作者或 AI 完成任务后，应提交到任务分支，并发起 Pull Request 指向 `dev`。
- PR 合并前必须由仓库主人或指定维护者审核。
- PR 标题和说明必须使用中文，描述要能让不参与本次编码的人理解改动。
- 每个 PR 尽量只解决一个明确问题，避免混入无关重构、格式化和资源变更。
- 如果修改影响移动端、日志、渲染、输入、存档、脚本执行或核心兼容逻辑，PR 说明中必须写清风险。

操作日志要求：

- 每次代码任务都要保留中文操作日志，至少写在最终回复或 PR 描述中。
- 日志必须包含：任务目标、修改文件、关键改动、执行命令、验证结果、未验证内容、风险和回退建议。
- 如果使用用户提供的日志排查问题，必须记录日志文件名、日志日期、关键错误、推断链路和对应修复点。
- 如果没有运行测试或无法导出 APK，必须明确说明原因，不能写成已经验证。

PR 描述建议模板：

```markdown
## 目标

说明本次要修复或实现什么。

## 修改内容

- 修改了哪些文件
- 关键逻辑变化
- 是否更新 CODE_MAP.md

## 验证

- 执行过的命令
- 桌面端验证结果
- Android/APK 验证结果
- 未验证内容和原因

## 风险与回退

- 可能影响的模块
- 兼容性风险
- 回退建议
```

多 AI 协作规则：

- 每个 AI 只处理自己任务范围内的文件，不要顺手改无关问题。
- 如果任务需要跨模块修改，先用 `CODE_MAP.md` 定位边界，再说明为什么必须跨模块。
- 同一轮任务中如果发现用户或其他 AI 已修改相关文件，要以最新文件为准，不要用旧上下文覆盖。
- 合并冲突不要盲目解决；涉及核心逻辑、存档兼容、移动端行为时，应交给仓库主人确认。
- 任何 AI 都不能把“代码能编译”当成唯一完成标准，必须结合本项目的 Android/APK 和 Emuera 兼容目标判断。

## 协作规则

- 不要猜测用户意图。需求不清楚、日志不完整、复现条件矛盾时，先提问。
- 如果用户只要求分析，不要擅自改代码。
- 如果用户要求修复，默认需要完成定位、实现、验证和结果说明。
- 遇到已有未提交改动时，不要回滚用户改动；只处理与当前任务相关的文件。
- 每次最终回复要说明：改了什么、为什么改、是否验证、是否更新 `CODE_MAP.md`。
