# gEmuera 第四轮综合任务 Plan

```
topology: multi-module
change-set: gemuera-round4-20260805
coordinator: E:/MyCode/GodotCode/gEmuera-future
spec: docs/staging/specs/2026-08-05-gemuera-round2.md（沿用铁律）+ 本轮决策见下
```

执行模式：goal 模式，Workflow 分阶段，阶段间验证 gate。铁律：语义零漂移（对照 v24/Snake）。

## 阶段 A：bug 修复组（任务 3 + 任务 5）

```
goal: 修复 pinch zoom 双指缩放 + 语言切换后游戏内提示不刷新的 i18n bug。
```

- [ ] A1 pinch zoom：systematic-debugging 调查触摸链（HandleContentTouchGesture/HandleContentScreenTouch/HandleContentScreenDrag/BeginContentPinch/HandleContentPinchGesture/SetContentScaleKeepingFocus，EmueraContent.cs:7171-7360）→ 定位缺陷 → 修复。验证：双指缩放方向/范围/焦点/与拖动不冲突。
- [ ] A2 i18n：根因=语言切换后已建 UI 文本不刷新（msgBox 按钮/等待提示/菜单等 _Ready 一次性设置）。修复：MultiLanguage 加 LanguageChanged 通知 + EmueraContent/OptionWindow 实现文本刷新。验证：切换 zh_cn 后游戏内提示（msgBox/等待/菜单）立即变中文。

## 阶段 B：任务 4 settingPanel 排版

```
goal: OptionWindow（设置面板）重新排版——分组标题、间距、滚动、深色主题一致。
```

- [ ] B1 重排 OptionWindow：设置项分组（显示/快捷/输入/系统）、分组标题、分隔、内边距、内容超出时滚动（ScrollContainer）、语言切换后 label 刷新（联动 A2）。

## 阶段 C：任务 1 第三轮性能优化

```
goal: 实施第三轮扫描的 N1-N6（高）+ M1-M10 精选 + 遗留 P0（M4/5 动效回归修复）。
```

- [ ] C1 P0 修复：WirePressAnimation Tween Kill 复用（GEmueraTheme.cs:221）+ QuickButtons 遗留
- [ ] C2 N2 音频 LRU 缓存（EmueraContent.LoadAudioStream）
- [ ] C3 N1 tickRedrawTimer 冗余刷新（EmueraConsole.cs:1232）
- [ ] C4 N4 GDRAWCIMG Duplicate 消除（GraphicsImage.cs:198）
- [ ] C5 N5 启动并行化（ConstantData CSV 并行 + ERB 批量加载）
- [ ] C6 N3 GDRAWSTRING LRU + 批量（EmueraTextRenderComponent）
- [ ] C7 M1-M10 中收益精选（按验证后取舍）

## 阶段 D：任务 2 组件化扩展

```
goal: 更多功能组件化（Godot 组合优于继承），一个组件多场景复用。
```

- [ ] D1 分析候选（渲染组件、输入组件、诊断组件等）→ 选出可复用组件
- [ ] D2 实施组件化（.tscn + 接口 + 挂载），行为不变

参考：docs/staging/specs/2026-08-05-gemuera-round2.md（语义铁律）；v24/Snake 参考源码。
