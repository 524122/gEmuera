# 跨文档冲突台账与防回归规则

“关闭”表示当前 FrameworkDesign 已采用单一权威定义，不表示运行时代码/fixture 已通过。

| ID | 历史冲突 | 统一裁决 | 权威文档 | 状态 |
| --- | --- | --- | --- | --- |
| C-01 | 多套存档都称同一主格式 | Upstream1808 与 GEmueraSnake 为显式 codec profile；冲突类型码禁止自动猜测；ERAS 另为独立 Extension | SaveFormat | Closed decision |
| C-02 | RESTART=返回标题/清变量 | 当前 frame JumpTo ParentLabelLine | ScriptEngine | Closed |
| C-03 | 把统一语义 runner 误等同于全部路径必须立刻 Step/Resume | M2–M5 保留专用线程 Legacy runner；resumable runner 仅在 yield audit 后替换 | ScriptEngine/ExecutionContract | Closed decision |
| C-04 | Core 独立但暴露 Godot 类型 | 纯 .NET csproj + ports/effects | DependencyGraph | Closed |
| C-05 | 7 个全局 manager | 最多四个应用 Autoload；会话服务归 Session | Architecture | Closed |
| C-06 | RichText/自绘/逐行节点均为最终 | Display 固定；后端 ADR 待原型 | RenderingSystem | Closed as decision state |
| C-07 | basename 扫描 vs CSV | AppContents CSV 声明驱动 | ResourceSystem | Closed |
| C-08 | UPCHECK=跳过/按键检查 | PALAM update 与清零 | InputSystem | Closed |
| C-09 | 编码打分 vs strict UTF-8 fallback | EncodingHelper 算法 | EncodingSystem | Closed |
| C-10 | Timer 能发现主线程卡死 | 只承诺外部/platform watchdog | ErrorRecovery | Closed |
| C-11 | async void 切换/音频 | Task+CTS+generation | StateIsolation/AudioSystem | Closed |
| C-12 | managed heap=总内存 | 分 managed/native/GPU/media | LifecycleMemory | Closed |
| C-13 | 曾把实现类名与公共名混淆 | 上游注册 CBGSETSPRITE；旧 gEmuera 还注册 CBGSETCIMG alias；按 profile 分开 | EvidenceIndex/InstructionInventory | Closed |
| C-14 | 曾把“当前上游工作区无 Godot 工程”误写成所有基线无工程 | 分开上游工作区、旧 gEmuera Godot 4.7 工程和未来目标 lock | HowToRun/GEmueraBaseline | Closed fact; target evidence open |
| C-15 | 固定 30/60 FPS/内存数字互斥 | 统一 measurement protocol/targets | PerformanceOptimization | Closed |
| C-16 | 移动外部目录权限旧模型 | SAF/content URI、iOS security scope | HowToRun/SecurityLimits | Closed design, unverified |
| C-17 | 只审计上游 XEmuera，遗漏旧 gEmuera 修复 | upstream + legacy + game fixture 三层矩阵 | GEmueraBaseline/EvidenceIndex | Closed design |
| C-18 | 主线程 cooperative VM 被提前 Accepted | 专用 VM thread 默认；主线程仅 full yield audit 后实验 | Architecture/ExecutionContract | Closed decision |
| C-19 | 简化 Display DTO 被 Accepted | DTO v2 必须含 DivPart、srcb、事务/scroll/data-only | KeyClasses/RenderingSystem | Closed design, golden pending |
| C-20 | GraphicsImage 像素真相不明 | Session PixelStore 为唯一 CPU truth；GPU 为 revision 投影 | ExecutionContract/ResourceSystem | Closed decision |
| C-21 | Switch gate 包围长加载导致新请求无法取消旧请求 | generation/CTS 在 gate 外发布；gate 只包短 commit | StateIsolation/MigrationPlan | Closed |
| C-22 | 音频 crossfade/voice stealing 被当透明适配 | 默认 compat mode 关闭；仅 Extension 启用 | AudioSystem | Closed design |
| C-23 | 单对象安全上限足以防 OOM | device tier + session aggregate + atomic reservation | SecurityLimits | Closed design, device data pending |
| C-24 | 结束任务删除所有测试 vs 永久差分资产 | 临时诊断可删；正式 regression/fixture/golden 永久保留 | ProjectStructure/MigrationPlan | Closed governance |
| C-25 | “目标设计可实施”被误解为可直接重写 | 当前唯一实施基线是 M0–M2 且串行放行；M3+ 只作规划 | M0M2ImplementationBaseline/MigrationPlan | Closed decision; evidence open |
| C-26 | DTO 边界被误解为同时重写 layout/renderer | M2.0 先旁路深复制且旧 renderer 直连；M2.1 才 canary adapter | M0M2ImplementationBaseline/RenderingSystem | Closed decision; golden pending |
| C-27 | 无 profile sidecar 时由 codec 试读自动猜测 | 默认只读阻断并提示显式 profile；dry-run 副本；转换备份且不原地覆盖 | SaveFormat/SaveLoadSystem | Closed decision; tool missing |
| C-28 | SAF 目标路径被误解为可立即替换旧 Android 路径 | M0–M2 保持旧路径；SAF 后续独立 canary、真机门禁和回退 | SecurityLimits/M0M2ImplementationBaseline | Closed decision; device evidence open |
| C-29 | “进度可合并”被泛化为 Display/结果消息可丢弃 | committed transaction/REDRAW/WAIT/INPUT/completion/fault 保序；仅非语义进度或有 fixture reducer 的项可合并 | ExecutionContract/M0M2ImplementationBaseline | Closed decision; behavior inventory open |
| C-30 | runner 宿主 timeline 与各业务诊断各自编号，无法证明跨线程总顺序 | M0 legacy trace 使用单一有界 recorder 分配全局 sequence；timeline/effects/errors 只作 trace 投影，normalizer 不删除或重排 sequence | VerificationPlan/M0M2ImplementationBaseline | Closed implementation rule; cross-fixture evidence open |
| C-31 | “本机有游戏/源码目录”被误当成 fixture 已获授权且可随仓库分发 | fixture manifest 分开记录 availability/content hash 与 authorization/redistribution；授权未验证时只能本地引用，缺失 root/report 必须为 Uncovered | ProjectStructure/HowToRun/M0M2ImplementationBaseline | Closed implementation rule; license review open |
| C-32 | headless 空/无渲染帧或单一后端截图被误当成双后端视觉/hit 证据 | headless 截图明确 Uncovered；可视截图必须在 `frame_post_draw` 后采集；Controls/Canvas 分目录并校验 effective backend；hit 单独走真实 backend 入口 | VerificationPlan/M0M2ImplementationBaseline/RenderingSystem | Closed implementation rule; eraFL/device evidence open |
| C-33 | 单次复杂页 Captured 或三次都 exit 0 被误写成可重复稳定 | RepeatCount 门禁要求 semantic projection 可重复；当前 plan-binding repeat3 的 semantic/screenshot/hit 均已采集且 semantic 一致，但 raw transport trace 每次漂移，必须保留原始 trace、不得删除结果类 event、重排 sequence 或用归一化造绿 | VerificationPlan/ExecutionContract/M0M2ImplementationBaseline | Closed evidence rule; transport batching root-cause and feature coverage open |
| C-34 | 依据 `SNAKE_*` 类型名、当前方法名或源码枚举顺序直接决定未来模块归属/覆盖顺序 | M0-DIA 类型名只记 provenance warning；公开键按 ordinal canonical 化；重复键失败；DIA-07 上游同名也只增加 provenance，模块归属必须由 BehaviorKey/capability、replacement 声明和两侧 fixture 裁决 | DialectExtensionSystem/InstructionInventory | Closed inventory rule; 131 ownership unresolved and 686 alias/replacement contracts open |
| C-35 | 测试 registry projection 的未选择模块不变性通过，被误写为旧 runtime 已完成 D2 隔离 | 报告必须并列 `testProjectionInvariant=Passed` 与 `currentRuntimeIsolation=Failed`；只有旧静态字典被候选 frozen registry 取代且两侧行为 snapshot 通过后才能关闭 D2 | DialectExtensionSystem/M0M2ImplementationBaseline | Closed evidence wording; runtime isolation open |
| C-36 | 静态扫描到单一 ReturnType/ArgBuilder 或 `WaitInput`，就被写成正式 signature/completion 兼容 | M0-DIA-03 分开 Resolved/Conditional/Unresolved，completion/effect 强制 `StaticCandidate + behaviorFixtureStatus=Uncovered`；正式 descriptor 需逐公开键求值和两侧 trace/fixture | DialectExtensionSystem/InstructionInventory | Closed evidence rule; descriptor behavior open |
| C-37 | 按 handler 类型给全部共享注册填同一参数/返回值，或让宽正则静默吞掉未来新增 key | M0-DIA-04/05 规则必须以 handler+exact/锚定 regex 唯一命中，固定 expectedMatchCount，且选择值必须属于 DIA-03 候选；stale/ambiguous/duplicate/已解析项命中一律失败 | DialectExtensionSystem/InstructionInventory | Closed static resolution rule; behavior evidence open |
| C-38 | 把类体扫描到的所有 `flag` 赋值直接 OR 成 descriptor，导致局部变量、注释和互斥构造分支混入 | DIA-06 分开 registration/single/multi；多分支只接受 known flag additive rules，规则贡献必须存在于 DIA-03 候选，TWAIT/AWAIT/INPUTMOUSEKEY 噪声有固定回归 | DialectExtensionSystem/InstructionInventory | Closed effective-flag rule; runtime flag behavior open |
| C-39 | 把 DIA-07 的上游同名或当前模块候选计数写成最终 ownership/兼容结论，或让上游同名自动覆盖当前 target | `UpstreamNameMatch` 只提供 provenance；`ExplicitCurrentModuleCandidate` 仍是候选；冲突单独记录且不自动 replacement。只有 comparer/alias/replacement、两侧行为 fixture 与 runtime frozen registry 闭合后才能裁决 ownership/D2 | DialectExtensionSystem/InstructionInventory/CompatibilityMatrix | Closed evidence wording; 131 ownership unresolved and runtime isolation Failed |
| C-40 | 把 DIA-08 捕获的旧 lookup 行为直接写成 D2 comparer/alias/replacement 已解决，或把 `_Rename.csv` 文本替换当 registry alias | DIA-08 只记录 `ICVariable` 静态 comparer 捕获、`ICFunction` current-culture `ToUpper` 风险、9 个碰撞的旧指令优先和 `_Rename` 的词法分析前 `SourceTextRewrite`；未来 D2 必须显式选择 comparer/normalizer，semantic `AliasOf`/`ReplacementDeclaration` 仍需逐 key 裁决和 fixture | DialectExtensionSystem/InstructionInventory/CompatibilityMatrix | Closed lookup evidence rule; 686 semantic alias/replacement unresolved, runtime Failed/NotConsumed |
| C-41 | 把 DIA-09 的 `V24VisibleCandidate`/`SnakeOnlyCandidate` 当成最终模块归属、替换意图或行为兼容，或据此删除 current contribution 的交叉证据 | DIA-09 只机械读取 DIA-02 test projection、DIA-07 ownership 与 DIA-08 lookup：131 未决项=123 v24-visible+8 Snake-only，且保留 v24/Snake moduleId/currentContribution；`CALLSHARP`、TOOLTIP 和中文 key 等仍必须由 alias/replacement、BehaviorKey 与两侧 fixture 裁决 | DialectExtensionSystem/InstructionInventory/CompatibilityMatrix | Closed visibility evidence wording; ownership/semantic/runtime remain open |
| C-42 | 将 central static root 的源码 inventory 当成已完成 M1 会话隔离，或把 `GlobalStatic.Reset()` 文本命中当作并发/异步清理验证 | M0-SES-01 只锁定 `GlobalStatic=14`、`Program=17`、14 个中心 reset site（12 baseline + canary-only `ctrlZ` 与 `UEMUERA_DEBUG` 下 `StackList`）和 479 个直接引用；`StackList` 的清理证据仅适用于 debug canary。范围外 static、真实 reset order、thread safety、generation/stale completion、A→B→A、façade/flag/rollback 仍须独立报告。报告必须保持 runtime isolation Failed / M1 Blocked | StateIsolation/M0M2ImplementationBaseline/CompatibilityMatrix | Closed evidence wording; M1 implementation and evidence open |
| C-43 | 将 DIA-10 的静态 profile→projection 预检、semantic hash 或 `SnakeModernMobile` 拒绝误报为运行时多版本解释器、M1 `CompatibilityPlan` 或 SnakeModernMobile 兼容 | DIA-10 只读 DIA-02/M0-SES-01，且仅 `v24pure`/`snake` 有证据投影；semantic hash 故意排除 selection source/generation，完整 set hash 只证明静态可复现。`SnakeModernMobile=Uncovered`，runtime isolation Failed、Parser 已有 descriptor registry presence guard 但 handler/typed-policy behavior 仍 `NotConsumed`、D1 plan behavior `NotImplemented`、M1 Blocked 必须原样保留；startup/parser identity hand-off 是独立增量证据 | DialectExtensionSystem/StateIsolation/CompatibilityMatrix | Closed static-preflight wording; runtime resolver, D2 registry and behavior evidence remain open |
| C-44 | 把 DIA-11 的旧 marker/launcher precedence 当成可信内容识别、运行时 resolver，或因 `IsSnakeProfile` 包含 modern 分支而把 `SnakeModernMobile` 视为 Snake 兼容 | DIA-11 只证明当前源码选择顺序和两个 marker 集；launcher/runner 仅允许 v24pure/snake，modern 分支 marker-only、DIA-10 Uncovered、runner Unsupported。未来 resolver 必须有 manifest/fingerprint/pin/capability/ambiguity 处理与独立 fixture，不能复用旧全局 `CoreProfile` | DialectExtensionSystem/CompatibilityMatrix/EvidenceIndex | Closed source-selection wording; content resolver, runtime plan and modern-profile compatibility remain open |
| C-45 | 把 M0-DIA-12 的静态 CompatibilityPack catalog 当作可执行游戏 manifest、可信内容识别或已可分发的多版本解释器 | DIA-12 只钉扎 DIA-10/11 的 `v24pure`/`snake` 静态证据，module 必须内置且 capability 字段必须显式；`legacy.current.*` 仅 projection support。未知 module、SnakeModernMobile、DLL/type/script/path/URL 均失败；content=`NotBound`、save/fixture=`Uncovered`、distribution=`Blocked` | DialectExtensionSystem/CompatibilityMatrix/EvidenceIndex | Closed declaration-boundary rule; fingerprint/pin/manifest resolver/runtime plan/D2 registry and behavior evidence remain open |
| C-46 | 把 DIA-13 的 `StaticCandidate` BehaviorKey/CapabilityId 当作已验证的 policy 值、runtime capability 或可写入当前 CompatibilityPack 的 manifest allowlist | DIA-13 只把 DIA-01 的当前 30 classification/97 branch hit 聚合为可追溯词汇；每项锁定 module/fixture/provenance 并统一 `NotEligible`。工具反向检查 DIA-12 pack exposure 仍为空；任何值、绑定、行为或 allowlist 都必须等待两侧 fixture 和独立运行时阶段 | DialectExtensionSystem/CompatibilityMatrix/EvidenceIndex | Closed static-vocabulary wording; policy semantics, runtime capability binding, manifest resolver and D2 evidence remain open |
| C-47 | 把 DIA-14 的 future consumer contract/decision owner 草案当作已经实现的 policy manager，或让 scoped configuration input 与 future catalog consumer 横向直接耦合 | DIA-14 只锁定 10 boundary、14 classification、16 file binding 及 `DecisionConsumer`/`ConfigurationInput` role。每个决策键只能有一个 consumer contract/owner；scoped config schema 不作最终决策，registration guard 才是 catalog decision consumer。所有项 `BoundaryDraftOnly`/`NotImplemented`，DIA-13 exposure 仍为空 | DialectExtensionSystem/CompatibilityMatrix/EvidenceIndex | Closed static-boundary wording; policy values, manager/resolver/plan/D2/Parser/VM and two-sided behavior evidence remain open |
| C-48 | 将 DIA-15 的 future `portTypeId` 当作已存在的 C# interface/方法签名，或继续使用与机器 vocabulary 不一致的下划线 key、`IStartupParseFaultPolicy`、`IPrivateArgumentPolicy` 等旧草案名 | DIA-15 以 DIA-14 的唯一 consumer/owner 为准，固定 10 个 port=6 PolicyDecision+2 BridgeProjection+2 FrozenCatalogContribution；每项 `InterfaceDraftOnly`、value=`Unspecified`、runtime type=`NotImplemented`。port 只分配未来实现位置，不授权旧 Parser/VM/manifest 调用 | DialectExtensionSystem/CompatibilityMatrix/EvidenceIndex | Closed static-port wording; C# API review, method/DTO/default, policy manager/resolver/plan/D2/Parser/VM and fixture behavior evidence remain open |
| C-49 | 将 DIA-16 的两个静态 module descriptor、dependency-first closure 或 `[1.0.0,2.0.0)` 版本范围误写成已安装模块、运行时 resolver/catalog、可加载 assembly 或已完成多版本分发 | DIA-16 只钉扎 DIA-12/15 的 source hash，固定 `gemuera.v24` 无依赖/port、`game.snake` 依赖前者并拥有 10 port；拓扑序只保证 stable DTO order。任何 runtime assembly/type scan、version-range resolution、content/manifest/pin、capability/save/fixture binding、CompatibilityPlan/D2/Parser/VM 都必须由后续阶段和两侧证据关闭 | DialectExtensionSystem/CompatibilityMatrix/EvidenceIndex | Closed static-composition wording; runtime catalog/resolver/plan, module loading, distribution and session-isolation evidence remain open |
| C-50 | 将 M0-SAV-01 的 source enum/header/hash 或发现 `Float=0x20..0x23` 误写为原始存档已验证、自动 SaveProfile 选择、codec 可运行或安全的原地 load | M0-SAV-01 仅锁定 five-source wire facts，0x20–0x22 冲突强制 `StaticConflictObserved`/`automaticSelectionStatus=Unbound`。真实 `.sav/.dat`、offset map、四 file type/normal+gzip、profile conflict、content pin 和 read/write round-trip 必须独立 fixture；新架构仍要求 read-only candidate parse、explicit decision 与 atomic commit | SaveFormat/SaveLoadSystem/CompatibilityMatrix/EvidenceIndex | Closed static-save wording; fixture, runtime SaveProfile/codec/candidate commit and M1 isolation evidence remain open |
| C-51 | 将 M0-DIA-17 的 fixture requirement 当成已捕获行为、已可实现的 C# port，或向 catalog 填入默认值/DTO/可执行载荷以绕过两侧差分 | DIA-17 只能复用 DIA-13/15 的 source fixture 和 port；每项固定 v24pure/Snake、baseline/extension/undeclared、input/result/error/completion/effect，且全部 `Planned`/`Uncovered`/`BlockedByFixture`。catalog 严格拒绝未知/重复 key、hash/fixture 漂移、提前 Captured 与 policy value/method/DTO/runtime payload | DialectExtensionSystem/CompatibilityMatrix/EvidenceIndex | Closed fixture-gate wording; actual two-sided traces, C# API review, runtime policy/plan/D2/Parser/VM and M1 isolation remain open |
| C-52 | 另一条 AI 并行完成 M0-M2 时，M3-M7 规划文档被误当作可直接实施，或覆盖 M0-M2 的状态/报告 | M3-M7 文档只定义后续边界；M0-M2 仍是当前唯一实施基线，阶段状态拆成 `NotStarted / Blocked / PreviousGate:*`；并行修改导致 source/report identity 变化时，后续报告必须重新生成 | MigrationPlan/M0M2ImplementationBaseline/AcceptanceTraceability | Closed planning rule; M0-M2 gate and all M3-M7 evidence remain open |
| C-53 | 把 Godot Node/Texture/RID 当作 Core 所有者，或用 `queue_free`/GC 代替 session/generation 清理 | Core 只拥有逻辑状态和 PixelStore CPU truth；Godot 对象由主线程 Bridge 按 generation/revision 投影；退出先 detach/cancel/disconnect/kill tween，再释放 Resource/RID/Node 并核对 ledger | M3CoreExtraction/M4ResourceGraphics/M6SchedulingRenderingExperiment/LifecycleMemory | Closed lifecycle rule; runtime leak/device reports open |
| C-54 | 将 erafl 通过一个 HTML 截图误写成全部魔改 Emuera 兼容，或复制 `if (EraFL)` 分支 | erafl 作为跨 Parser/VM/Input/Resource/Display/Android 的第二基线；可复用能力用 CompatibilityPack/CapabilityId/typed policy 表达，未有双侧 fixture 的组合保持 `Uncovered` | M3CoreExtraction/M5PlatformComposition/M6SchedulingRenderingExperiment/M7CleanupReleaseGovernance/DialectExtensionSystem | Closed compatibility governance; full erafl/other-mod evidence open |

| C-55 | 将已实现的 M1-CORE-01/02、最小 `LegacySessionFacade`、默认关闭 canary 或首等待 cross-ABA observation 误写成 M1 已通过，或反过来否认合同/rollback smoke 证据 | M1-CORE-01/02 提供确定性 module/plan 合同、profile→内置 module 目录、lease/generation guard、Godot legacy backend 的 stop/start-before-commit 顺序、默认 false canary、跨 game/profile A→B→A 首等待观察和一次 100-switch lifecycle observation；后者仍只是 runner-only 证据，Snake working-set 预算风险未关闭。Parser 已接线 descriptor registry presence/module guard，但仍没有 handler/typed-policy behavior consumption、完整 static isolation、输入中切换/Android 证据。M1 阶段必须保持 `InProgress / Blocked / PreviousGate:M0`，不能放行 M2 | M1CoreRuntimeContractSlice/M0M2ImplementationBaseline/MigrationPlan/AcceptanceTraceability/EvidenceIndex/CompatibilityMatrix/KnownLimitations | Closed wording; memory budget, real runtime isolation, real canary rollout, Parser/VM behavior, Android and gate approval remain open |

| C-56 | M3–M7 各阶段已有目标和门禁，但缺少统一的工单记录、报告字段、实验阈值和删除 packet，导致不同维护者可能把“写完设计”误作“可放行” | M3–M7 统一使用版本化 work package、输入 identity、命令/exit code、差分/生命周期/回退/uncovered 报告和独立 gate decision；阶段语义仍由各自文档定义，当前 M0–M2 授权边界不变 | M3M7EngineeringExecution/MigrationPlan/AcceptanceTraceability | Closed documentation governance; all M3–M7 runtime evidence remains open |

| C-57 | 将 TDD 误解为每一次 AI 编辑都必须跑全量真实游戏、可视 Godot、APK、性能和阶段签署，或反过来因反馈慢而删除测试 | 验证分为 Explore、FastLoop、WorkPackage、PhaseRelease；纯确定性 Core 使用定向测试，遗留语义先特征化，真实游戏/设备保留为升级门。快速反馈不改变阶段或兼容结论 | AIDevelopmentWorkflow/M0M2ImplementationBaseline/VerificationPlan/HowToRun | Closed workflow decision; test infrastructure and runtime evidence remain open |

## 主题所有者

术语索引=Glossary；术语/所有权权威=Architecture；当前实施范围=M0M2ImplementationBaseline；依赖=DependencyGraph；VM=ScriptEngine；原版格式=SaveFormat；I/O=SaveLoadSystem；资源=ResourceSystem；HTML=MarkupSystem；输入=InputSystem；渲染=RenderingSystem；安全上限=SecurityLimits；验证状态=AcceptanceTraceability。

## 防回归检查

全文守卫检查互斥格式名、同步脚本旁路、Godot 类型进入 Core、会话服务 Autoload、固定渲染最终方案、旧存储权限、把静态 lookup 或 visibility candidate 证据夸大为 D2/semantic alias-replacement/ownership 完成，以及无证据绝对化表述。发现新定义时必须链接主题所有者，不复制第二套表。

每次修改任务要求同时更新 EvidenceIndex、CompatibilityMatrix、KnownLimitations、AcceptanceTraceability 和本台账中受影响项。
| C-58 | Do not treat `CompatibilityPlan` identity binding at startup/parser boundary as descriptor or policy execution | Backend binds one immutable plan before worker start; `Program` and `ParserMediator` verify profile/hash and clear it on canary reset, while legacy registrations remain behavior owner | M1CoreRuntimeContractSlice/CompatibilityMatrix/AcceptanceTraceability | Closed identity-boundary wording; descriptor behavior, D2 registry, full isolation and device evidence remain open |
| C-59 | A local save-file hash/header audit is not an original-format compatibility or round-trip proof | The fixture audit is shared-read only, records candidate bytes and 1808 header offsets, and keeps gzip/profile/codec/round-trip status Uncovered | SaveFormat/SaveLoadSystem/EvidenceIndex | Closed evidence wording; full save fixture and codec evidence remain open |
| C-60 | A post-binding 100-switch semantic match is not a leak-budget or full-isolation pass | The refreshed report preserves 101 lifecycle samples, repeated fingerprints and zero fixture mutations while retaining the ~667 MB working-set risk and uncovered input/late-completion/static/device evidence | M1CoreRuntimeContractSlice/EvidenceIndex/KnownLimitations | Closed observation wording; M1 gate remains blocked |
| C-61 | A source change that adds plan identity references invalidates all downstream DIA hashes even when runtime behavior is intentionally unchanged | Regenerate DIA-01 then DIA-03..17 in dependency order; generated reports and their contracts are authoritative, while all statuses remain Partial/Blocked | DialectExtensionSystem/EvidenceIndex/InstructionInventory | Closed evidence-refresh rule; gate status unchanged |
| C-62 | Three repeat runs with equal semantic projections, screenshots and hit probes prove stable display behavior or authorize dropping differing transport traces | Plan-binding repeat3 keeps both backends' semantic/screenshot/hit evidence captured, but transport trace hashes differ on every run. Audit localizes drift to `ui_projection/apply_text_changes` batching (`GenericUtils.FlushUI` frame/96-action/7ms budget plus worker completion order); retain raw transport artifacts and keep M0-DSP-01 blocked until a deterministic scheduler/barrier contract or explicit review decision exists | EvidenceIndex/CompatibilityMatrix/KnownLimitations | Closed display-repeat evidence rule; transport stability, nested feature coverage and device/signature evidence remain open |
